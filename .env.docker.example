# Copy to .env.local for local Docker Postgres (no Supabase required)
DATABASE_URL=postgresql://deckvault:deckvault@localhost:5432/deckvault

# Optional — leave empty to use Demo Mode in the browser
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

POKEMON_TCG_API_KEY=
NEXT_PUBLIC_GITHUB_REPO=
NEXT_PUBLIC_APP_VERSION=0.2.0
